interface Asset {
    path: string;
    content: string;
}
interface PathBuffer {
    path: string;
    content: Buffer;
}
type AssetOrPathBuffer = Asset | PathBuffer;
interface DocumentInput {
    html: string;
    title?: string;
    test?: boolean;
    assets?: AssetOrPathBuffer[];
    save?: boolean;
    /**
     * Number of seconds to cache the file in the CDN for.
     */
    expiresIn?: number;
}
declare class Onedoc {
    private apiKey;
    private endpoint;
    constructor(apiKey: string, endpoint?: string);
    private buildUrl;
    render(document: DocumentInput): Promise<any>;
}

declare class HtmlBuilder {
    private title?;
    private start;
    private middle;
    private end;
    constructor(title?: string);
    build(document: string, styleSheets?: string[]): string;
}

export { HtmlBuilder, Onedoc };
